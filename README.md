# tokenator
This project is a Lexical Analyzer which is used to break down input into smaller, meaningful units called tokens. It serves as the first stage of a compiler or interpreter, analyzing the input and generating a stream of tokens for further processing.
